# Exporters

This directory contains Dockerfiles for the metrics exporters used in the observability stack.

Currently included:

- `node-exporter/`: host metrics
- `cadvisor/`: Docker container metrics
